import Checkout from '@/components/checkout/Checkout'

export const metadata = {
	title: 'EasyTech | Checkout',
	description: 'This is the checkout page.',
}

const page = () => {
	return (
		<>
			<Checkout />
		</>
	)
}

export default page
